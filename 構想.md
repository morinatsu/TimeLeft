構想：
機能としてはタイマー。
* 横長で、時間経過とともにバーが減っていくUI。
* インプットは下記の2つ
  - 入力欄。分(数値)か、時刻( `:`で区切られた時・分)のいずれかを入力。時刻が入力された場合は、その時刻までの時間を分で表示する。
  - Start兼End(Reset)ボタン。(一時停止はしない)
* スタートとともに、入力欄だった場所に残り時間を分単位で表示。
* 時間経過を表すバーは緑(開始直後)から赤(終了間近)へグラデーション。背景は黒で分割ラインも欲しい。10個に分割。
* タイマーのウィンドウを閉じるボタンはほしい。
* 黒の背景全体を枠で囲って、拡張機能自体の大きさは時間経過とともに変わることのないように。
